from packages.maths import *

from packages.subpackages.sorted import sorted
# Example usage of the sorted function
li = [5, 2, 9, 1, 5, 6]
sorted_list = sorted(li)
print(f"Sorted list: {sorted_list}")